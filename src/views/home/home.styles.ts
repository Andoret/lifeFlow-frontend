import { theme } from "../../theme";
export const homeStyles = {
    container: {
        height: '100%',
        width: '100vw',
        background: 'linear-gradient(45deg, ' + theme.palette.primary.main + ' 30%, ' + theme.palette.primary.dark + ' 95%)',
        padding: '20px',
        paddingBottom: '30px',
    }
}